.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install anagram

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv anagram
    $ pip install anagram
